{% extends 'layouts/base.html.twig' %}

{% block title %}
	Profile {{ app.user.fullName }}
{% endblock %}

{% block body %}
	<div class="row">
		<div class="col-md-6 mx-auto">
			<h1>Profile</h1>
			<p>{{ app.user.email }}</p>
			<p>{{ app.user.fullName }}</p>

			<a href="{{ path('app_account_edit') }}">Edit account</a> | <a href="">Change password</a>
		</div>
	</div>
{% endblock %}
